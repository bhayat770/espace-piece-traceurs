{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - Espace pièces traceurs{% endblock %}

{% block content %}
    <div class="container">
        <h1>Modifier mon mot de passe</h1>
    </div>
    <div class="container">
        <a href="{{ path('app_account') }}">Retourner à mon compte</a><br/>
    </div>
    <hr>

    {# {{ if notification }}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %} #}
    <div class="d-flex justify-content-center"> {# Use the "d-flex" class to enable flexbox #}
        <div class="custom-form-wrapper"> {# Add a custom class to style the wrapper #}
            {{ form(form) }}
        </div>
    </div>
    <hr>
{% endblock %}
