{% extends 'base.html.twig' %}

{% block content %}
    <h1>Produits de la catégorie {{ category.name }} avec le tag {{ tag }}</h1>

    <ul>
        {% for product in products %}

            <li>{{ product.name }}</li>
            <a href="{{ path('app_product', {'slug': product.slug}) }}">{{ product.name }}</a>
        {% endfor %}
    </ul>

{% endblock %}
